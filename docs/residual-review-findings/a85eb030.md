# Residual Review Findings — ce-code-review `ms-spec-driven` (2026-07-08)

Source report: `.devnotes/ce-code-review/report-ms-spec-driven-20260708.md`
Resolution pass base commit: `a85eb030`

## Deployment Assumption (Governs Deferred Auth Hardening)

Pilot deployment model agreed for this pass: **single backend worker process + Postgres, with the
backend port network-isolated (only the Next.js middleware proxy can reach it)**. Findings #12,
#14, and #15 are deferred under this assumption. If the backend port is ever exposed or worker
processes are scaled out, re-triage those findings before deploying.

## Applied In This Pass

| # | Sev | Fix |
| --: | --- | --- |
| 1 | P0 | `frontend/src/proxy.ts` renamed to `frontend/src/middleware.ts`; wiring test added |
| 2 | P0 | `with_for_update(skip_locked=True)` on all three worker claim queries (`domains.py`, `indexing.py`, `sources.py` — report listed two; sources was the same pattern) |
| 3 | P1 | Delete worker finalizes domain-gone exit as `succeeded`; generation-fence exit as `cancelled` |
| 4 | P1 | Turn finalization is compare-and-set guarded on `status='running'`; late streams cannot overwrite redaction |
| 5–8 | P1 | Wiki DTOs aligned to API-001: `wikiPageId`, `publishedAt` (+ `wiki_revisions.published_at` column and `(wiki_page_id, published_at DESC)` index per DATA-001), `publishedPageId`/`publishedRevisionId`, evidence-ref `id` |
| 9 | P1 | Verified existing 401 chain (backend 401s disabled users; client flips auth store; layout redirects); frontend test added. Idle-page visual staleness accepted for pilot |
| 10 | P1 | Migration 0007 downgrade now raises instead of silently dropping `audit_events`/`request_id` correlation data |
| 11 | P1 | Tests added for delete-worker domain-gone and generation-fence terminal states |
| 13 | P2 | `Settings` rejects `samesite=none` without `secure=True` (and invalid samesite values) |
| 16 | P2 | `GET /wiki/pages` ordered by newest update; `ix_wiki_pages_state_updated` index aligned to DATA-001 |
| 18 | P2 | `ceFetch` no longer sets `Content-Type: application/json` on `FormData` bodies |
| 19 | P2 | Invalidated wiki evidence refs return null `citationLabel`/`sourceLabel` |
| 20 | P2 | `is_absent` raises a safe `SourceIndexError` instead of swallowing exceptions as "still present" |
| 21 | P2 | Index worker fails a claimed source with no `request_id` instead of leaving it stuck SUBMITTING |
| 26 | P2 | Wiki `state` columns given `server_default` in amended migration 0008 |
| 29 | P2 | Density (and theme) preferences applied at bootstrap via `data-density`/`data-theme`; comfortable density tokens added |
| 30 | P2 | OpenAPI snapshot regenerated for P11 wiki routes; diff reviewed (additions are the 10 wiki routes only, no unsafe fields) |
| 31 | P2 | P4 boundary test sharpened to AST import checks; contract-mandated `/diagnostics/lightrag` route path no longer false-positives |

Migration 0008 was amended in place (greenfield branch, no deployed database) rather than adding a 0009.

## Deferred With Reason

| # | Sev | Reason |
| --: | --- | --- |
| 12 | P2 | Login rate limiting — deferred under isolated-port assumption above |
| 14 | P2 | `/docs` exposure — deferred under isolated-port assumption above |
| 15 | P2 | CORS policy — deferred under isolated-port assumption; same-origin proxy is the only caller |
| 17 | P2 | Admin wiki contributions pagination — schedule in pilot hardening sprint |
| 22 | P2 | SQLite wiki circular-FK skip — accepted dev-only limitation (report Open Decision 2, explicitly deferred, not dropped) |
| 23 | P2 | Audit check-constraint swap atomicity — migration-safety follow-up |
| 24 | P2 | `conversation_turns.domain_id` FK-less vs spec — needs spec decision (report Open Decision 3, explicitly deferred, not dropped) |
| 25 | P2 | UniqueConstraint vs Index autogenerate drift — cleanup follow-up |
| 27 | P2 | Geist font loading — bundle with a DESIGN.md visual-parity pass |
| 28 | P2 | Settings dialog focus trap / Escape — a11y follow-up |
| 32–43 | P3 | Advisory items — accept for pilot; revisit in hardening sprint |

## Verification

- `.venv/bin/pytest tests/` → 99 passed, 0 failed (was 94 passed, 2 failed; 4 tests added)
- `frontend npm test` → 10 passed (was 7; 3 tests added)
- Fresh SQLite migration to head succeeds; downgrade past 0007 raises as intended
