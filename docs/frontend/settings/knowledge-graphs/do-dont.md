# Knowledge Graphs — do / don't

## Do

- Follow [`../../shared/accordion-storage-kit.md`](../../shared/accordion-storage-kit.md) and this pack’s **target** grammar.
- Cite `.reference-ls-frontend/templates/nextjs-feature-demos/features/environment-controls/` while Controllers accordion/storage is **not exported yet**.
- Import shared UI from `@/components/ui` (`SettingsGroup`, `ProgressBar`, `StatusPill`, `UiModal`, …).
- Keep storage bars expand-only and backend-owned via `storageSummary`.
- Prefer factory target + product plans (006 / 002 / 003) over copying known-wrong live markup.

## Don't

- Invent Controllers accordion / storage primitives or claim they are already barrel exports.
- Copy hand-rolled live `SettingsPanel` drift as the pattern to teach the next route.
- Show ports, runtime URLs, container ids, compose targets, or storage paths.
- Compute storage in the browser or put storage bars on collapsed headers.
- Port foreign settings chrome or non-CE path trees into this surface.
- Broad-remediate live KG density inside a docs-only factory change — that belongs to the polish plan.
