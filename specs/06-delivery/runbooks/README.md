# Runbooks

Runbooks capture operational evidence and repeatable checks. Current active runbook:

- `pilot-launch.md`

Add new runbooks only when a release, support, migration, or incident process needs repeatable operator steps.
