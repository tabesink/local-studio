# Data Contracts

Primary data contract: `context-engine-data.md`.

Persistent data has one owner per concern. Do not add mirror tables, generic workflow/job tables, query logs, runtime manifest/env tables, or JSON settings buckets unless an approved feature changes the data contract first.
