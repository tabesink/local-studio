# Local Studio Token Notes

Import `local-studio-tokens.css` in the demo app root before rendering feature templates.

These tokens are intentionally a small compatibility subset of the reference token system from `frontend/src/app/styles/globals/tokens.css`. In a production rebuild, copy the full token file and theme runtime from Local Studio instead of extending this subset.
